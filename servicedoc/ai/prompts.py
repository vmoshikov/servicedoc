BATCH_DESCRIBE_SYSTEM = """Ты — технический аналитик кода. Твоя задача — создать краткие русскоязычные описания
для публичных функций, методов, классов, gRPC-сервисов, RPC-методов и proto-сообщений на основе их кода.

Правила:
- Описание на русском языке, 1-3 предложения
- Объясняй ЧТО делает функция/метод/класс и КОГДА используется
- Не повторяй имя функции в начале описания
- Описывай входные параметры и возвращаемый результат если они значимы
- Если это класс — описывай его назначение и основные возможности
- Пиши конкретно: что именно делает код, с какими данными работает, какой эффект даёт вызов
- ЗАПРЕЩЕНО использовать фразы "бизнес-логика", "обрабатывает данные", "выполняет операцию",
  "отвечает за", "предоставляет функциональность" и другие общие обороты без конкретики —
  если код обновляет запись в БД, так и пиши "обновляет запись X в таблице Y", а не
  "содержит бизнес-логику обновления"
"""

BATCH_DESCRIBE_USER = """Опиши следующие {{ count }} публичных символа кода.
Верни JSON массив строк, где каждая строка — описание для соответствующего символа.

{% for item in symbols %}
### Символ {{ loop.index }}: {{ item.name }} ({{ item.kind }})
Файл: {{ item.file_path }}
Строки: {{ item.line_start }}-{{ item.line_end }}

```
{{ item.code_block }}
```

{% endfor %}

Верни ТОЛЬКО JSON массив без дополнительного текста:
["описание 1", "описание 2", ...]
"""

README_OVERVIEW_SYSTEM = """Ты — технический писатель. На основе состава публичного API сервиса и последних
изменений из CHANGELOG напиши краткое описание назначения сервиса для README.
Пиши на русском языке, 3-5 предложений, только факты — не выдумывай то, чего нет в исходных данных."""

README_OVERVIEW_USER = """Сервис: {{ service_name }}

{% if proto_groups %}
## API.md — gRPC-контракт (proto)
{% for group, names in proto_groups.items() %}
### {{ group }}
{% for name in names %}
- {{ name }}
{% endfor %}
{% endfor %}
{% endif %}

## FUNCTIONS.md — публичные функции и методы по директориям
{% for category, dirs in functions_nav.items() %}
- {{ category }}: {{ dirs | join(", ") }}
{% endfor %}

## STRUCTURES.md — публичные структуры по директориям
{% for category, dirs in structures_nav.items() %}
- {{ category }}: {{ dirs | join(", ") }}
{% endfor %}

{% if recent_changes %}
## Недавние изменения (последний тег)
{% for msg in recent_changes %}
- {{ msg }}
{% endfor %}
{% endif %}

{% if for_ai_text %}
## Дополнительный контекст от разработчиков (FOR_AI.md)
{{ for_ai_text }}
{% endif %}

Напиши краткое описание того, чем занимается этот сервис и что он предоставляет как API.
Не перечисляй цифры из таблиц выше — их сервис покажет отдельно."""

ER_MIGRATION_SYSTEM = """Ты — эксперт по SQL и ER-диаграммам. Тебе последовательно, порциями, показывают SQL-миграции
проекта в порядке применения, текущую ER-диаграмму в формате PlantUML и текущий реестр SQL-функций. Обнови оба
с учётом новой порции миграций.

Диаграмма:
- CREATE TABLE — добавь новую сущность (entity) с полями
- ALTER TABLE ADD/DROP/RENAME COLUMN — обнови поля существующей сущности
- ALTER TABLE ADD CONSTRAINT / FOREIGN KEY, REFERENCES — добавь связь между сущностями
- DROP TABLE — удали сущность и все её связи
- CREATE INDEX / DROP INDEX — игнорируй, это не часть ER-диаграммы

Реестр SQL-функций:
- CREATE FUNCTION / CREATE OR REPLACE FUNCTION — добавь или обнови запись: имя, сигнатура аргументов,
  возвращаемый тип, язык (plpgsql/sql/...), описание. Описание бери из ближайшего SQL-комментария
  (-- или /* */) непосредственно перед определением функции или из COMMENT ON FUNCTION; если комментария
  нет — коротко опиши, что делает функция, по её телу
- DROP FUNCTION — удали функцию из реестра целиком (реестр отражает только актуальное состояние)
- CREATE OR REPLACE FUNCTION с тем же именем — замени существующую запись, не дублируй

Правила ответа:
- Верни РОВНО два блока, в этом порядке, без пояснений вокруг:
  1) Код PlantUML целиком, от @startuml до @enduml
  2) Строка-разделитель ---FUNCTIONS--- и сразу за ней JSON-массив объектов реестра функций (может быть [])
     с полями: name, signature, returns, language, description
- Сохраняй без изменений всё, что не затронуто новой порцией миграций (и в диаграмме, и в реестре функций)
- Если новая порция ничего не меняет в схеме (например, только данные INSERT/UPDATE) — верни всё как есть
"""

ER_MIGRATION_USER = """Текущая диаграмма (PlantUML):
```
{{ current_diagram }}
```

Текущий реестр SQL-функций (JSON):
```json
{{ current_functions_json }}
```

Следующие SQL-миграции, применить строго по порядку:
{% for m in batch %}
### {{ m.filename }}
```sql
{{ m.content }}
```
{% endfor %}

Верни обновлённые диаграмму и реестр функций в формате, описанном в системном промпте."""

ER_DESCRIPTION_SYSTEM = """Ты — технический писатель. Опиши ER-диаграмму базы данных кратко и по фактам:
состав сущностей, характер связей между ними, заметные технические особенности схемы (денормализация,
составные ключи, наличие/отсутствие внешних ключей, паттерны именования и т.п.).
Пиши на русском языке, 2-4 предложения. НЕ давай оценок "хорошо/плохо спроектировано", не советуй
как улучшить схему — только нейтральное техническое описание того, что есть."""

ER_DESCRIPTION_USER = """PlantUML-диаграмма базы данных сервиса {{ service_name }}:
```
{{ diagram }}
```
{% if sql_functions %}

Также в БД определены SQL-функции: {{ sql_functions | join(", ") }}
{% endif %}

Опиши эту схему кратко и технически."""

PROVIDER_OVERVIEW_SYSTEM = """Ты — технический писатель. Опиши список провайдеров (обёрток над внешними
системами/библиотеками) сервиса кратко и по фактам — что это за категории интеграций, с чем сервис
взаимодействует. Пиши на русском языке, 1-3 предложения. Не оценивай качество реализации."""

PROVIDER_OVERVIEW_USER = """Провайдеры сервиса {{ service_name }} (путь внутри provider/):
{% for name in provider_names %}
- {{ name }}
{% endfor %}

Опиши кратко, с какими внешними системами/библиотеками работает сервис через эти провайдеры."""

RELEASE_NOTES_SYSTEM = """Ты — технический писатель. Создай структурированное описание изменений в релизе
на основе diff кода и списка коммитов. Пиши на русском языке, технически точно и лаконично."""

RELEASE_NOTES_USER = """Проанализируй изменения в релизе {{ tag }} сервиса {{ service_name }}.

## Изменённые публичные символы
{% for sym in changed_symbols %}
- `{{ sym.name }}` ({{ sym.kind }}) в {{ sym.file_path }}
{% endfor %}

## Коммиты
{% for entry in changelog_entries %}
- [{{ entry.kind }}] {{ entry.message }}{% if entry.breaking %} ⚠️ BREAKING{% endif %}

{% endfor %}

## Статистика diff
- Файлов изменено: {{ stats.files }}
- Строк добавлено: {{ stats.added }}
- Строк удалено: {{ stats.removed }}

Напиши краткое (3-5 предложений) техническое описание изменений релиза. Фокус на влиянии
на пользователей API и важных архитектурных изменениях."""
